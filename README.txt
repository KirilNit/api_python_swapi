to run the tests:
-create venv python3
-pip install --upgrade pip
-pip install requirements.txt
-chose pytest as default test runner in Preferences > Tools > Python Integrated Tools > Default Test Runner